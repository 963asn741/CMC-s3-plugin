{
  "events": {
    "service_operation": {
      "s3p": {
        "files": {
          "create": {
            "name": "Create instance",
            "pending": "Creating instance {request.name}",
            "success": "File {request.name} was created",
            "failure": "Unable to create file {request.name}"
          }
        }
      }
    }
  }
}