{
  "events": {
    "service_operation": {
      "s3p": {
        "instances": {
          "create": {
            "name": "Create instance",
            "pending": "Creating instance {request.name}",
            "success": "Instance {request.name} was created",
            "failure": "Unable to create instance {request.name}"
          }
        }
      }
    }
  }
}